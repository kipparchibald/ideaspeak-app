# Production demo (set in Vercel → Project → Settings → Environment Variables)
# Get from https://console.x.ai/
XAI_API_KEY=xai-your-key-here

# Local dev: copy to .env and run `bun run dev:full`
# The Bun server at server/index.ts proxies all /api/* calls securely