# Active project: IdeaSpeak only

This workspace is **IdeaSpeak** (`ideaspeak-app`) — the voice-first xAI app builder.

## Portfolio (never jumble)
1. **IdeaSpeak** → this repo (`ideaspeak-app`)
2. **SummitForge** → `SummitForge-RE-OS` only
3. **Split Rock Construction** → `split-rock-construction` only

Do not mix brands, code, status, or deploys. See `PROJECTS.md`.
